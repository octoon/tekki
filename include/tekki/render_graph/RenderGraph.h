#pragma once

// This is a compatibility header that redirects to the actual render graph implementation
#include "tekki/render_graph/graph.h"
#include "tekki/render_graph/pass_builder.h"
#include "tekki/render_graph/resource.h"

// No additional definitions needed - RenderGraph is already defined in graph.h
