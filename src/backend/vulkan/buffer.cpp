#include "tekki/backend/vulkan/buffer.h"

namespace tekki::backend::vulkan {

// Buffer implementation is now inline in the header
// Device buffer methods are implemented in device.cpp

} // namespace tekki::backend::vulkan
